{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window's capabilities. Semantic commands only \u2014 no shell, no arbitrary filesystem (migration report section 43).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "notification:default",
    "deep-link:default"
  ]
}
