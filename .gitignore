# OS
.DS_Store

# Dependencies. The packages live in .deps; node_modules is a symlink into it,
# because the resolvers insist on that name — see scripts/deps-dir.sh
.deps/
node_modules
**/node_modules/
package-lock.json

# Build output
dist/
target/
**/build/
**/gen/android/app/build/

# Signing / secrets
**/*.der
**/*.pem
**/*.keystore
**/*.jks
.env
.env.*
!.env.example

# IDE
.idea
*.iml
